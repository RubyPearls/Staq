<div class="well" >
  <%= question.title %>
  <%= question.content %>

  <hr>
  by: <strong><%= link_to question.user.email, question.user %></strong> <%= time_ago_in_words(question.created_at) %> ago
</div>
