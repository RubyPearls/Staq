<div id="answers">
  <%= form_for [@question, @answer], remote: true do |f| %>
  <%= f.label :content %>
  <%= f.text_area :content %>
  <%= f.submit %>
  <% end %>
</div>

<a href="/">Return to main page</a>
